import 'package:flutter/material.dart';
import 'package:financeapp/features/shared/app_shell.dart';

class FileReportsDashboardScreen extends StatelessWidget {
  const FileReportsDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return AppShell(
      title: 'File Reports Dashboard',
      body: const Center(child: Text('File Reports Dashboard — Coming Soon')),
    );
  }
}
