import 'package:flutter/material.dart';
import 'package:financeapp/features/shared/app_shell.dart';

class AdvanceDealerScreen extends StatelessWidget {
  const AdvanceDealerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return AppShell(
      title: 'Advance Dealer',
      body: const Center(child: Text('Advance Dealer — Coming Soon')),
    );
  }
}
