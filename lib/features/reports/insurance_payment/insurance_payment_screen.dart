import 'package:flutter/material.dart';
import 'package:financeapp/features/shared/app_shell.dart';

class InsurancePaymentScreen extends StatelessWidget {
  const InsurancePaymentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return AppShell(
      title: 'Insurance Payment',
      body: const Center(child: Text('Insurance Payment — Coming Soon')),
    );
  }
}
