import 'package:flutter/material.dart';
import 'package:financeapp/features/shared/app_shell.dart';

class RtoPaymentScreen extends StatelessWidget {
  const RtoPaymentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return AppShell(
      title: 'Rto Payment',
      body: const Center(child: Text('Rto Payment — Coming Soon')),
    );
  }
}
